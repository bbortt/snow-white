logging:
  level:
    org.apache.kafka.clients.admin.internals.AdminMetadataManager: WARN
spring:
  application:
    name: openapi-coverage-stream
  kafka:
    streams:
      application-id: openapi-coverage-stream
  profiles:
    default: prod
