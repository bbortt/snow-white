springdoc:
  packages-to-scan: io.github.bbortt.snow.white.microservices.quality.gate.api.api.rest
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  profiles:
    default: prod
