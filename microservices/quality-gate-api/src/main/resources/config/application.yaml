springdoc:
  packages-to-scan: io.github.bbortt.snow.white.microservices.quality.gate.api.api.rest.resource
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  profiles:
    default: prod
