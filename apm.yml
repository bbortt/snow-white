#
# Copyright (c) 2026 Timon Borter <timon.borter@gmx.ch>
# Licensed under the Polyform Small Business License 1.0.0
# See LICENSE file for full details.
#

# APM (Agent Package Manager) manifest -- https://microsoft.github.io/apm
#
# This publishes the `snow-white` Claude Code skill so consumers can install it with
# `apm install bbortt/snow-white#<tag>` instead of copying SKILL.md by hand.
#
# The skill source of truth is `.apm/skills/snow-white/`. The copy under
# `.claude/skills/snow-white/` is generated -- run `pnpm run skill:sync` after editing.

name: snow-white
version: 1.9.1-SNAPSHOT # x-release-please-version
description: Read and act on Snow-White JUnit XML quality gate reports -- turn failing API coverage criteria into prioritized test improvements.
author: Timon Borter
license: PolyForm-Small-Business-1.0.0
type: skill

# Pinned so `apm pack` output is deterministic in CI rather than following whichever
# harness the last contributor had installed.
targets:
  - claude

includes: auto
