<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2026 Timon Borter <timon.borter@gmx.ch>
  ~ Licensed under the Polyform Small Business License 1.0.0
  ~ See LICENSE file for full details.
  -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.bbortt.snow-white</groupId>
  <artifactId>parent</artifactId>
  <version>1.11.0</version>

  <packaging>pom</packaging>

  <scm>
    <url>scm:git:https://github.com/bbortt/snow-white.git</url>
  </scm>

  <modules>
    <module>examples/example-snow-white-openapi-generator</module>
    <module>examples/example-spring-boot</module>
    <module>microservices</module>
    <module>toolkit</module>
    <module>internal</module>
    <module>helm</module>
  </modules>

  <properties>
    <!-- Global -->
    <java.version>25</java.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- SonarQube properties -->
    <sonar.projectName>Snow White</sonar.projectName>
    <sonar.host.url>http://localhost:9000</sonar.host.url>

    <sonar.sources>src/main</sonar.sources>
    <sonar.tests>src/test</sonar.tests>

    <sonar.inclusions>src/main/**/*.java</sonar.inclusions>
    <sonar.exclusions>src/main/resources/**</sonar.exclusions>

    <sonar.test.inclusions>src/test/**/*.java</sonar.test.inclusions>

    <sonar.coverage.exclusions>
      examples/**,
      src/main/java/clew/traceables/**,
      **/Main.java
    </sonar.coverage.exclusions>

    <!-- JaCoCo properties -->
    <jacoco.version>0.8.15</jacoco.version>
    <sonar.java.coveragePlugin>jacoco</sonar.java.coveragePlugin>
    <sonar.coverage.jacoco.xmlReportPaths>
      ${maven.multiModuleProjectDirectory}/target/jacoco-aggregate/jacoco.xml
    </sonar.coverage.jacoco.xmlReportPaths>
    <sonar.dynamicAnalysis>reuseReports</sonar.dynamicAnalysis>
    <sonar.junit.reportPaths>
      target/surefire-reports,
      target/failsafe-reports
    </sonar.junit.reportPaths>

    <!-- Dependency Management -->
    <jackson.version>3.2.2</jackson.version>
    <jackson-v2.version>2.22</jackson-v2.version>
    <junit.version>6.1.3</junit.version>
    <license-maven-plugin.version>2.7.1</license-maven-plugin.version>
    <maven-failsafe-plugin.version>3.6.0</maven-failsafe-plugin.version>
    <maven-surefire-plugin.version>3.6.0</maven-surefire-plugin.version>
    <mockito.version>5.23.0</mockito.version>
    <org.projectlombok.version>1.18.48</org.projectlombok.version>
    <pitest-junit5.version>1.2.3</pitest-junit5.version>
    <pitest.version>1.30.0</pitest.version>
    <sonar-maven-plugin.version>5.8.0.7211</sonar-maven-plugin.version>
    <spring-boot.version>4.1.1</spring-boot.version>
    <spring-retry.version>2.0.13</spring-retry.version>

    <!-- Node.js & NPM -->
    <frontend-maven-plugin.version>2.0.2</frontend-maven-plugin.version>
    <node.downloadRoot>https://nodejs.org/dist/</node.downloadRoot>
    <node.version>v24.15.0</node.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <dependency>
        <groupId>io.opentelemetry.semconv</groupId>
        <artifactId>opentelemetry-semconv</artifactId>
        <version>1.44.0</version>
      </dependency>

      <dependency>
        <groupId>io.github.bbortt.snow-white</groupId>
        <artifactId>archunit-rules</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.github.bbortt.snow-white</groupId>
        <artifactId>commons</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>

      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <version>${org.projectlombok.version}</version>
        <scope>provided</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>com.github.eirslett</groupId>
          <artifactId>frontend-maven-plugin</artifactId>
          <version>${frontend-maven-plugin.version}</version>
          <configuration>
            <installDirectory>target</installDirectory>
            <nodeVersion>${node.version}</nodeVersion>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.16.0</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-dependency-plugin</artifactId>
          <version>3.11.0</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven-failsafe-plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>3.6.1</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>**/*AppTest.java</exclude>
            <exclude>**/*IT.java</exclude>
            <exclude>**/*IT$*.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <configuration>
              <includes>
                <include>**/*IT.java</include>
              </includes>
            </configuration>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <version>${license-maven-plugin.version}</version>
        <configuration>
          <outputDirectory>${project.basedir}</outputDirectory>
          <thirdPartyFilename>MAVEN_LICENSES.txt</thirdPartyFilename>
          <fileTemplate>
            /org/codehaus/mojo/license/third-party-file-groupByLicense.ftl
          </fileTemplate>
          <excludedGroups>io.github.bbortt</excludedGroups>
          <excludedScopes>test,provided</excludedScopes>
          <sortArtifactByName>true</sortArtifactByName>
        </configuration>
      </plugin>

      <plugin>
        <!--
          Not inherited: this resets the shared jacoco.exec exactly once, at the very start of
          the reactor (the root project always builds first, scoped or not), so a coverage check
          never mixes data with a previous, unrelated invocation. Bound to validate so it runs
          before jacoco:prepare-agent (initialize).
        -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.2.0</version>
        <inherited>false</inherited>
        <executions>
          <execution>
            <id>reset-shared-jacoco-exec</id>
            <phase>validate</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <delete file="${maven.multiModuleProjectDirectory}/target/jacoco.exec"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <!--
            Mirrors sonar.coverage.exclusions above (clew trace markers, Spring Boot bootstrap
            classes), plus generated code Sonar never sees because it lives outside src/main
            (sonar.inclusions is src/main-scoped) but JaCoCo's bytecode-based check still bundles:
            OpenAPI-generated client SDKs and server stubs/DTOs, MapStruct mapper implementations,
            and OpenTelemetry's generated protobuf messages. None of these carry hand-written logic
            to test. The generator also stamps them @jakarta.annotation.Generated, but that
            annotation is @Retention(SOURCE) - it never reaches the .class file, so JaCoCo's own
            built-in @Generated filter cannot see it either; these excludes are the only thing that
            works.
          -->
          <excludes>
            <exclude>clew/traceables/**</exclude>
            <exclude>**/Main.class</exclude>
            <exclude>**/api/client/**</exclude>
            <exclude>**/api/rest/dto/**</exclude>
            <exclude>**/api/rest/*Api.class</exclude>
            <exclude>**/ApiUtil.class</exclude>
            <exclude>**/*MapperImpl.class</exclude>
            <exclude>io/opentelemetry/proto/**</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>jacoco-initialize</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <destFile>
                ${maven.multiModuleProjectDirectory}/target/jacoco.exec
              </destFile>
            </configuration>
          </execution>
          <execution>
            <id>jacoco-report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
            <configuration>
              <dataFile>
                ${maven.multiModuleProjectDirectory}/target/jacoco.exec
              </dataFile>
              <outputDirectory>
                ${project.reporting.outputDirectory}/jacoco
              </outputDirectory>
            </configuration>
          </execution>
          <execution>
            <id>check-coverage</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <dataFile>
                ${maven.multiModuleProjectDirectory}/target/jacoco.exec
              </dataFile>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.9</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.9</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.sonarsource.scanner.maven</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>${sonar-maven-plugin.version}</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <!--
        Application tests exercise a published image over the network: the code under test runs
        inside a container, while the JaCoCo agent is attached to the local failsafe JVM. No
        production class is ever loaded in the instrumented JVM, so the shared jacoco.exec comes
        out empty and `check-coverage` reports a 0.0 covered ratio for the whole bundle.

        Coverage is measured and gated by the regular `verify` build, where the code actually runs
        in-process; re-running the same gate here can only ever fail. Disabling the plugin outright
        (rather than just the check) also keeps the apptest run from truncating the reactor-wide
        jacoco.exec that the coverage build writes.

        Inherited: every module that defines its own `apptest` profile merges with this one by id.
      -->
      <id>apptest</id>

      <properties>
        <jacoco.skip>true</jacoco.skip>
      </properties>
    </profile>

    <profile>
      <id>node</id>

      <build>
        <plugins>
          <plugin>
            <groupId>com.github.eirslett</groupId>
            <artifactId>frontend-maven-plugin</artifactId>

            <executions>
              <execution>
                <id>install-node-and-npm</id>
                <goals>
                  <goal>install-node-and-npm</goal>
                </goals>
                <configuration>
                  <nodeVersion>${node.version}</nodeVersion>
                  <nodeDownloadRoot>${node.downloadRoot}</nodeDownloadRoot>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>sonar</id>

      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>${jacoco.version}</version>
            <executions>
              <execution>
                <id>report-aggregate</id>
                <phase>verify</phase>
                <goals>
                  <goal>report-aggregate</goal>
                </goals>
                <configuration>
                  <dataFileIncludes>
                    <dataFileInclude>**/jacoco.exec</dataFileInclude>
                  </dataFileIncludes>
                  <excludes>
                    <exclude>clew/traceables/**</exclude>
                    <exclude>**/Main.class</exclude>
                    <exclude>**/api/client/apiindexapi/**</exclude>
                    <exclude>**/*MapperImpl.class</exclude>
                    <exclude>io/opentelemetry/proto/**</exclude>
                  </excludes>
                  <outputDirectory>
                    ${maven.multiModuleProjectDirectory}/target/jacoco-aggregate
                  </outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.sonarsource.scanner.maven</groupId>
            <artifactId>sonar-maven-plugin</artifactId>
            <version>${sonar-maven-plugin.version}</version>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!--
        Mutation testing with PIT: `./mvnw -P mutation verify`. Not bound by default because a
        full mutation run takes far longer than the regular test suite. Inherited reactor-wide
        (rather than duplicated per module) is safe: pitest-maven skips 'pom'-packaged modules
        and any module with no production code or tests on its own, so a reactor-wide `verify`
        only ever mutates modules that actually have something to mutate.

        No targetClasses/targetTests are set here, so a full run mutates every class the module
        contains (see pitest-maven docs: with none configured, it scans the module's own output
        directory) - that's what the branches CI pipeline runs, unscoped, on every push.

        For fast local iteration, scope a run to only what changed on the branch instead of the
        whole module: `.github/scripts/pitest-changed-classes.sh` diffs against `main` (plus
        uncommitted changes) and re-invokes this profile per affected module with -DtargetClasses
        set to just those classes - targetClasses/targetTests are read from the -D system property
        directly by pitest-maven, so no extra wiring is needed here for that to work.
      -->
      <id>mutation</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>${jacoco.version}</version>
            <executions>
              <execution>
                <id>check-coverage</id>
                <phase>verify</phase>
                <goals>
                  <goal>check</goal>
                </goals>
                <configuration>
                  <skip>true</skip>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>${pitest.version}</version>
            <executions>
              <execution>
                <id>mutation-coverage</id>
                <phase>verify</phase>
                <goals>
                  <goal>mutationCoverage</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <mutationThreshold>80</mutationThreshold>
              <testStrengthThreshold>80</testStrengthThreshold>
              <thresholdPrecision>1</thresholdPrecision>
              <!-- Mirrors the jacoco excludes above, in PIT's class-name glob syntax: generated
                code (OpenAPI clients/stubs/DTOs, MapStruct implementations, protobuf messages)
                carries no hand-written logic, but PIT mutates it all the same and counts every
                one of those mutants as uncovered. Without this the reported score says nothing
                about the tests - it says how much generated code a module happens to contain. -->
              <excludedClasses>
                <param>clew.traceables.*</param>
                <param>*.Main</param>
                <param>*.api.client.*</param>
                <param>*.api.rest.dto.*</param>
                <param>*.api.rest.*Api</param>
                <param>*.ApiUtil</param>
                <param>*MapperImpl</param>
                <param>io.opentelemetry.proto.*</param>
              </excludedClasses>
              <!-- Mirrors the surefire excludes above: apptest/IT-style tests need Docker and
                real infrastructure, so they don't take part in a mutation run. PIT globs match
                against the (simple) class name, not a file path, unlike surefire's excludes. -->
              <excludedTestClasses>
                <param>*AppTest</param>
                <param>*AppTest$*</param>
                <param>*IT</param>
                <param>*IT$*</param>
              </excludedTestClasses>
              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>
              <!-- Explicit (though it matches the plugin default) so the report path stays
                stable for CI upload: without this it lands in target/pit-reports/<timestamp>/. -->
              <timestampedReports>false</timestampedReports>
              <timeoutFactor>2</timeoutFactor>
            </configuration>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>${pitest-junit5.version}</version>
              </dependency>
              <!--
                PIT runs tests on its own, isolated tool classpath, not the module's - without
                this it falls back to whatever (older) JUnit Platform version pitest-junit5-plugin
                itself was built against, rather than the version this reactor actually tests
                with (junit.version/junit-bom above), which JUnit 6 unifies with junit-platform.
              -->
              <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-launcher</artifactId>
                <version>${junit.version}</version>
              </dependency>
            </dependencies>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/bbortt/snow-white</url>
    </repository>
  </distributionManagement>
</project>
